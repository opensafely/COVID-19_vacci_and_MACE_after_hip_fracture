version: "4.0"

actions:

# delect --dummy-table row when submitting the code

  generate_dataset:
    run: ehrql:v1 generate-dataset analysis/dataset_definition.py
      --dummy-tables dummy-tables 
      --output output/dataset.csv.gz
    outputs:
      highly_sensitive:
        dataset: output/dataset.csv.gz