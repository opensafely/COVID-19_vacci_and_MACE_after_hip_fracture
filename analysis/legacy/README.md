# Historical Stata scripts

These are James Webster's original scripts from base commit b730777, retained
for reference with trailing whitespace removed. The active project uses R; these files are not run by project.yaml.
